{
  "name": "babymonitor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "shared",
    "api",
    "web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run dev --workspace=api & npm run dev --workspace=web",
    "build": "npm run build --workspace=shared && npm run build --workspace=api && npm run build --workspace=web",
    "test": "npm run test --workspace=shared && npm run test --workspace=api && npm run test --workspace=web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
